# A module is a file that contains Python code (functions, variables, classes) which can be reused in other programs.
# Modules provided by Python itself.
def sum(a,b):
    return(a+b)
def mul(c,d):
    return(c*d)